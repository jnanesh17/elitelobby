{
  "version": 2,
  "installCommand": "corepack pnpm install",
  "buildCommand": "corepack pnpm --filter ./artifacts/web run build",
  "builds": [
    {
      "src": "artifacts/web/package.json",
      "use": "@vercel/next"
    }
  ]
}
