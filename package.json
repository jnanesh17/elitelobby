{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "corepack pnpm --filter ./artifacts/web run dev",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "build:web": "corepack pnpm --filter ./artifacts/web run build",
    "dev:web": "corepack pnpm --filter ./artifacts/web run dev",
    "start:web": "corepack pnpm --filter ./artifacts/web run start",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
